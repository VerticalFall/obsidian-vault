# 操作系统 / 编辑器杂项
.DS_Store
Thumbs.db
desktop.ini

# Obsidian 本地状态(如果 Obsidian 在此子文件夹生成配置,不上传)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# 本地渲染预览(不上传)
_preview/


# Python
__pycache__/

# 本地计划任务脚本(不上传)
.scripts/

# 每日精选(LLM 产物,每天新增一份;目录由 render_daily_digest.py 自动创建)
# 同 _preview/:属于本地消费层产物,不入库,避免每天给仓库添一个文件
_每日精选/

# 选题池重建提案/备份(本地审核用,审核完即可删)
_选题池_提案.md
_选题池.md.bak

